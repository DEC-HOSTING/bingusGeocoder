# requirements.txt
Flask>=2.0
pandas>=1.3
openpyxl>=3.0
geopy>=2.2
python-dotenv>=0.19 # Good for local .env use
openai>=1.0
pytz>=2023.3
gunicorn>=20.0     # Required for Render web service
whitenoise>=6.0    # For serving static files
# Ensure all other dependencies are listed